{
  "name": "iron-and-oak",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "bun run build && wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "cf:status": "../scripts/cf-portfolio.sh status iron-and-oak",
    "preview": "astro preview",
    "astro": "astro",
    "lint": "oxlint",
    "format": "oxfmt",
    "prepare": "husky",
    "lint:fix": "oxlint --fix . && oxfmt",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^6.1.5"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0",
    "wrangler": "^4.81.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "oxlint --fix",
      "oxfmt"
    ]
  }
}
